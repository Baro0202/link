worker_processes  auto;
error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

events {
  worker_connections  4096;
  multi_accept on;
}

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;
  sendfile        on;
  tcp_nopush      on;
  tcp_nodelay     on;
  keepalive_timeout  65;
  server_tokens off;

  upstream backend_upstream {
    zone backends 64k;
    server backend:3000 max_fails=3 fail_timeout=5s;
  }
  upstream nextjs_upstream {
    zone nexts 64k;
    server nextjs:3000 max_fails=3 fail_timeout=5s;
  }

  map $request_method$request_uri $is_shorten {
    default 0;
    ~^POST/api/shorten$ 1;
  }

  server {
    listen 8080;
    listen [::]:8080;

    location /api/shorten {
      proxy_pass http://backend_upstream;
      proxy_http_version 1.1;
      proxy_set_header Connection "keep-alive";
      proxy_set_header Host $host;
    }

    location /healthz {
      proxy_pass http://backend_upstream;
      proxy_http_version 1.1;
      proxy_set_header Connection "keep-alive";
      proxy_set_header Host $host;
    }

    # Short codes and ticketbox prefix handled by backend
    location ~ ^/(ticketbox/)?[0-9A-Za-z]+$ {
      proxy_pass http://backend_upstream;
      proxy_http_version 1.1;
      proxy_set_header Connection "keep-alive";
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Everything else goes to Next.js UI
    location / {
      proxy_pass http://nextjs_upstream;
      proxy_http_version 1.1;
      proxy_set_header Connection "keep-alive";
      proxy_set_header Host $host;
    }
  }
}


